# Compact synthetic fleet replay

T04 and T09, 12–14 September 2026 UTC. This sample contains only brake air supply and axle bearing telemetry, scores, events and injection models. It lets a fresh checkout run the two research demos without the large ignored research datasets. Regenerate it with `python scripts/build_demo_data.py` after restoring the full local data.
