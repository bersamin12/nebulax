You are the independent final auditor for the W1 "foundation" stage of the NEBULA X Track 3 project (predictive fault detection on rolling stock: doors, brake air supply, axle-box bearings). Repository root: /mnt/berstorage/nebulax. Python interpreter for everything: /home/administrator/miniconda3/envs/nebulax/bin/python (run pytest as `<python> -m pytest -q`). Do not install packages, do not commit, do not modify files: this is a read-and-run audit. You may write scratch scripts under /mnt/berstorage/nebulax/results/verify/scratch/.

Read first: the approved plan at /home/administrator/.claude/plans/we-can-do-level-tingly-glade.md (sections "Common data schema", "Level 2 - Simulators", "Model benchmark"), docs/research/rail_phm.md (physics corrections that override the plan), docs/parameters.md (every simulator constant with its provenance), docs/provenance.md.

What W1 produced: nebulax/schema.py (the data contract), nebulax/sim/{common,door,pneumatic,bearing,selftest}.py, nebulax/adapters/{metropt3,cranfield,ottawa,validate}.py, nebulax/features/{windows,stats,vibration,cycles}.py, nebulax/bench/{base,registry}.py, scripts/{download_data,generate,calibrate_door,calibrate_pneumatic,calibrate_bearing}.py, tests/, data/raw/ (MetroPT-3, MetroPT-2, Ottawa, Cranfield complete: 13 .mat files + data description PDF), data/sim/ (10 trains x 30 days synthetic fleet), results/sim_checks/.

Audit checklist. For each item run it yourself and record the measurement in `checks[].detail`; put anything that must change before the model benchmark stage into `must_fix` (specific file, line or function, and what is wrong).

1. Test suite: run the full suite; report count passed/failed/skipped and total time. Any failure is must_fix.
2. Schema contract: write a scratch script that builds a small long-format frame with a wrong dtype, an unknown signal, and a NaN-only channel and confirm validate_long behaves as documented. Confirm to_wide/to_long round-trip and parquet round-trip preserve categoricals and NaN.
3. Simulator physics against the anchors in docs/parameters.md and rail_phm.md: (a) bearing healthy rise at 22.2 m/s and 0.5 load is 20-26 K and scales ~v^0.43 to 0.47; degradation at s=1 moves T_box by only a few K; hot_box fires the NS levels; (b) pneumatic healthy: band 8.06-10.21 bar, t_loaded ~110 s, t_unloaded ~416 s, current clusters ~0/3.8/5.9 A, TP2 high while loaded and low otherwise with H1 the opposite (this was a known inversion; confirm it is fixed); leak ramp reaches LPS; (c) door healthy closing 2-3.5 s, friction at s=1 lengthens closing and raises cruise current, obstruction logic reverses and retries. Time one simulated day per subsystem (budget < 10 s each).
4. Calibration reproducibility: run scripts/calibrate_pneumatic.py --no-plots and scripts/calibrate_bearing.py (with whatever no-plot flag exists) and confirm every constant row prints CONFIRMED; run scripts/calibrate_door.py and report its status (Cranfield may be partial: state which files are present). Check that the numbers in docs/parameters.md tables match what the scripts print now (spot-check at least 6 rows per subsystem).
5. Adapters: run `<python> -m nebulax.adapters.validate --source X --raw data/raw/X` for metropt3, ottawa and cranfield. Check MetroPT-3 sampling is treated as 10 s not 1 Hz, failure dates match the UCI/paper descriptor, Ottawa RMS/crest are AC-coupled (a record with a large DC offset must not have crest ~1), Cranfield tests and metadata counts match the files present.
6. Synthetic fleet: read data/sim/index.json and fault_log.parquet; confirm partitions on disk match the index, every run's telemetry and features validate, fault-log onset/failure times are inside the 30 days, ~40% of runs are healthy, and features carry the label columns the plan lists (fault_type, severity, rul_s, is_faulty, alarm_window_3d). Check for leakage-prone columns in the feature tables (anything that encodes severity or time-to-failure directly other than the label columns) and flag them.
7. Feature code: windows/stats/vibration/cycles: confirm window_stats column order matches feature_names, envelope spectrum picks the kurtogram band and returns BPFO/BPFI amplitudes, cycle features are keyed per the schema.
8. Leakage and honesty: look for any place a simulator or adapter writes ground-truth severity into a signal, any test that is trivially true, any hard-coded path outside the repo, any silent exception swallowing that hides failures, and any claim in docs/parameters.md that the code does not back.

This is the SECOND audit pass: the first pass (docs/audits/w1_audit.json, static-only because your sandbox could not start Python then) produced eight findings that have since been fixed: a meta_ metadata convention with nebulax.schema.feature_columns(), an observable shock estimator replacing the latent shock_count, MetroPT-3 per-cycle features aligned with the simulator plus transition_mask(), window splitting at logger gaps, NaN RUL for unknown failure times, envelope amplitudes scaled 2|E|/n, corrected docs claims, and a synthetic adapter (nebulax/adapters/synthetic.py, source 'sim'). Re-check each of those specifically in item 8 and confirm at runtime. Python now starts in your shell.

Set `passed` true only if items 1-6 have no must_fix. Put the key measured numbers in `measured` as a compact multi-line string.
