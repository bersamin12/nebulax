# Replace this file with the demo video

Record one screen capture of at most three minutes by following `docs/demo.md` in the source
repository. It must show all four tabs end to end: select/upload, run, inspect an on-screen
prediction and explanation, and download each CSV. Show the Door stream producing 38 segments,
the eight-car ACV ranking, the 68-file Rail folder completing in batches, and all 16 SHM files.

Before submission, delete this placeholder and place `demo_video.mp4` (or another organiser-
accepted video extension) beside `predictions.zip` and `app/`.
