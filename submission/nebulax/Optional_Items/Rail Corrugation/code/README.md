# Rail corrugation code

The runnable copy is in `../../../app/nebulax/ps3/rail.py` and `rail_features.py`. Shared scoring,
CSV validation and task contracts are in the adjacent `scoring.py`, `submission.py` and
`common.py`; `../../../app/scripts/ps3_predict.py` is the CLI/app prediction path.
